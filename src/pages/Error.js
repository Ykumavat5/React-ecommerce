import React from "react";

const Error = () =>{
    return (
        <div style={{ padding: "40px", textAlign: "center", color: "red" }}>
          <h2>⚠️ Something went wrong.</h2>
          <p>We’re working to fix the issue. Try refreshing the page.</p>
        </div>
      );
};

export default Error;